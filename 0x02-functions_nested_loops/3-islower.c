#include <stdio.h>

/**
* _islower - Checks if i is a lower case or not.
*
*/

int _islower(int i)
{
	int result;

	result = is
	if (i >= 97 && i <= 122)
		result = 1;
	else
		result = 0;

	return result;
}
